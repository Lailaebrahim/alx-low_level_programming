#include "main.h"
/**
 * print_sign - is  function that checks return absolute of a number
 * @n: int n takes the number to check
 * Return: absolute value of a number
 */
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
else
{
return (0);
}
}
