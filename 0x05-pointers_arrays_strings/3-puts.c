/**
 * _puts - a function that prints a string, followed by a new line, to stdout.
 * @str : pointer to a string
 */
void _puts(char *str)
{
int i = 0;
while (str[i] != '\0')
{
_putchar (s[i]);
i++;
}
}
